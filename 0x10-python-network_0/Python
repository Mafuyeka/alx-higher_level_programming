import requests

# Define the URL
url = 'https://intranet.hbtn.io/status'

# Send a GET request to the URL
response = requests.get(url)

# Display the size of the response body in bytes
print("Size of the response body:", len(response.content), "bytes")
